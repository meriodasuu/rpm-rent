import { deleteFaqAction, deleteServiceAction, saveFaqAction, saveServiceAction } from "@/app/admin/actions";
import { getStore } from "@/lib/data";

export const metadata = { title: "Контент" };

export default async function AdminContentPage({ searchParams }: { searchParams: Promise<{ saved?: string }> }) {
  const [query, store] = await Promise.all([searchParams, getStore()]);
  const [services, faqs] = await Promise.all([store.getServices({ includeHidden: true }), store.getFaqs({ includeHidden: true })]);
  return <div className="admin-page"><div className="container"><p className="eyebrow">Редактирование без кода</p><h1 className="admin-title">Услуги и FAQ</h1>{query.saved ? <p className="tag" style={{ marginBottom: 18 }}>Изменения сохранены</p> : null}<div className="grid-2"><div className="stack"><h2>Услуги</h2>{services.map((service) => <form className="surface admin-form" action={saveServiceAction} key={service.id}><input type="hidden" name="id" value={service.id} /><div className="stack"><div className="field"><label>Название</label><input className="input" name="title" defaultValue={service.title} /></div><div className="field"><label>Slug</label><input className="input" name="slug" defaultValue={service.slug} /></div><div className="field"><label>Описание</label><textarea className="textarea" name="description" defaultValue={service.description} /></div><div className="form-grid"><div className="field"><label>Цена</label><input className="input" name="price" type="number" defaultValue={service.price ?? ""} /></div><div className="field"><label>Порядок</label><input className="input" name="sortOrder" type="number" defaultValue={service.sortOrder} /></div></div><label className="checkbox"><input type="checkbox" name="published" defaultChecked={service.published} />Опубликована</label><div className="button-row"><button className="button" type="submit">Сохранить</button><button className="button ghost" formAction={deleteServiceAction} type="submit">Удалить</button></div></div></form>)}<form className="surface admin-form" action={saveServiceAction}><h3>Новая услуга</h3><input type="hidden" name="published" value="on" /><div className="stack"><input className="input" name="title" placeholder="Название" /><input className="input" name="slug" placeholder="slug" /><textarea className="textarea" name="description" placeholder="Описание" /><button className="button red" type="submit">Добавить</button></div></form></div><div className="stack"><h2>FAQ</h2>{faqs.map((faq) => <form className="surface admin-form" action={saveFaqAction} key={faq.id}><input type="hidden" name="id" value={faq.id} /><div className="stack"><div className="field"><label>Вопрос</label><input className="input" name="question" defaultValue={faq.question} /></div><div className="field"><label>Ответ</label><textarea className="textarea" name="answer" defaultValue={faq.answer} /></div><div className="form-grid"><div className="field"><label>Категория</label><input className="input" name="category" defaultValue={faq.category} /></div><div className="field"><label>Порядок</label><input className="input" name="sortOrder" type="number" defaultValue={faq.sortOrder} /></div></div><label className="checkbox"><input type="checkbox" name="published" defaultChecked={faq.published} />Опубликован</label><div className="button-row"><button className="button" type="submit">Сохранить</button><button className="button ghost" formAction={deleteFaqAction} type="submit">Удалить</button></div></div></form>)}<form className="surface admin-form" action={saveFaqAction}><h3>Новый вопрос</h3><input type="hidden" name="published" value="on" /><div className="stack"><input className="input" name="question" placeholder="Вопрос" /><textarea className="textarea" name="answer" placeholder="Ответ" /><button className="button red" type="submit">Добавить</button></div></form></div></div></div></div>;
}
